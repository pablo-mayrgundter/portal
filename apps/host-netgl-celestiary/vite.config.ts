import { defineConfig } from 'vite'

export default defineConfig({
  // Single HTML entry (index.html); the iframe target is celestiary itself,
  // served as a static asset out of public/celestiary/ (populated by the
  // `setup-celestiary` script — see package.json).
})
